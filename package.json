{
  "name": "northstar-stock-intelligence",
  "private": true,
  "type": "module",
  "scripts": {
    "refresh": "node --env-file-if-exists=.env scripts/refresh-data.mjs",
    "db:seed": "node --env-file-if-exists=.env scripts/seed-db.mjs",
    "serve": "node --env-file-if-exists=.env scripts/server.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node scripts/ensure-deps.mjs && node --check app.js && node --check scripts/db.mjs && node --check scripts/ensure-deps.mjs && node --check scripts/estimate-utils.mjs && node --check scripts/refresh-data.mjs && node --check scripts/seed-db.mjs && node --check scripts/server.mjs && node scripts/check-regressions.mjs && npm run test"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
